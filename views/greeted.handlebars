<div class="row">
    <h1>Greeted names: </h1>
    <ul>
        {{#each greeted}}<a href="/counter/{{name}}">
            <li>{{name}}</li>
        </a>
        {{/each}}
    </ul>

    <h3> {{greetedName}}</h3>

    <div>
        <a href="/">
            <button type="button" class="home">Home</button>
        </a>
    </div>
</div>